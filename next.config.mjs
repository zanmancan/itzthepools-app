// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // No experimental.appDir here — App Router is on by default in Next 14
};

export default nextConfig;
